# Multi-Aspect HMAC-Net 系統架構文檔

**版本**: 2.0
**更新日期**: 2025-01-12

---

## 📋 目錄

1. [系統總覽](#系統總覽)
2. [核心架構圖](#核心架構圖)
3. [模組詳解](#模組詳解)
4. [資料流程](#資料流程)
5. [創新點](#創新點)
6. [支援的任務類型](#支援的任務類型)

---

## 系統總覽

Multi-Aspect HMAC-Net 是一個**統一的情感分析框架**，支援：

- ✅ **Aspect-Based** 情感分析（SemEval）
- ✅ **Sentence-Level** 情感分析（IMDB、SST-2 等）
- ✅ 任何帶有文字的評論分析

### 核心特點

| 特點 | 說明 |
|------|------|
| **模組化設計** | BERT、AAHA、PMAC、IARM 獨立可替換 |
| **可學習的組合** | Selective PMAC 自動決定 aspects 是否組合 |
| **關係建模** | IARM 捕捉 aspects 間的交互關係 |
| **可解釋性** | Gate 值可視化、Attention 權重分析 |
| **通用性** | 自動適配不同資料集和任務 |

---

## 核心架構圖

### 1. Aspect-Based 任務架構（SemEval）

```
┌─────────────────────────────────────────────────────────────────┐
│                     INPUT (Multi-Aspect Sample)                 │
│  Text: "The food was great but service was slow"                │
│  Aspects: ["food", "service"]                                   │
│  Labels: [positive, negative]                                   │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BERT EMBEDDING LAYER                         │
│  ┌──────────────┐              ┌──────────────┐                │
│  │ Text Encoder │              │Aspect Encoder│                │
│  │  (DistilBERT)│              │  (DistilBERT)│                │
│  └──────┬───────┘              └──────┬───────┘                │
│         │                              │                         │
│         │  [batch, seq_len, 768]      │  [batch, aspects, 768] │
└─────────┼──────────────────────────────┼─────────────────────────┘
          │                              │
          └──────────┬───────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                    AAHA (Aspect-Aware Hierarchical              │
│                    Attention)                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Word-level Attention    → [batch, aspects, 768]         │  │
│  │  Phrase-level Attention  → [batch, aspects, 768]         │  │
│  │  Sentence-level Attention→ [batch, aspects, 768]         │  │
│  │                                                           │  │
│  │  Output: Context Vectors [batch, aspects, 768]           │  │
│  └──────────────────────────────────────────────────────────┘  │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│              PMAC (Progressive Multi-Aspect Composition)        │
│                                                                 │
│  ┌──────────────────┐         ┌───────────────────┐            │
│  │ Sequential PMAC  │   OR    │ Selective PMAC    │            │
│  │ (順序組合)        │         │ (可學習 Gate)      │            │
│  └──────────────────┘         └───────────────────┘            │
│                                                                 │
│  Selective PMAC 詳細流程:                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  For each aspect_i:                                      │  │
│  │    For each aspect_j (j ≠ i):                            │  │
│  │      ┌────────────────────────────────────────┐          │  │
│  │      │ Gate Network (MLP)                     │          │  │
│  │      │   Input: concat([aspect_i, aspect_j])  │          │  │
│  │      │   Output: gate_value ∈ [0, 1]          │          │  │
│  │      └────────────────┬───────────────────────┘          │  │
│  │                       │                                   │  │
│  │      ┌────────────────▼───────────────────────┐          │  │
│  │      │ Composition Network (MLP)              │          │  │
│  │      │   Input: concat([aspect_i, aspect_j])  │          │  │
│  │      │   Output: composed_feature             │          │  │
│  │      └────────────────┬───────────────────────┘          │  │
│  │                       │                                   │  │
│  │      influence = gate_value × composed_feature           │  │
│  │    aspect_i_final = aspect_i + Σ(influences)             │  │
│  │                                                           │  │
│  │  Output: [batch, aspects, 768] + Gate Matrix             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Gate Matrix Shape: [batch, aspects, aspects]                  │
│  Gate[i][j] = 影響程度 (aspect_i ← aspect_j)                   │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│              IARM (Inter-Aspect Relation Modeling)              │
│                                                                 │
│  ┌──────────────────┐    ┌──────────────┐    ┌──────────────┐ │
│  │ Transformer      │ OR │ GAT          │ OR │ Bilinear     │ │
│  │ (多頭注意力)      │    │ (圖注意力)    │    │ (雙線性交互)  │ │
│  └──────────────────┘    └──────────────┘    └──────────────┘ │
│                                                                 │
│  捕捉 aspects 之間的高階交互關係                                  │
│  Output: Enhanced Features [batch, aspects, 768]               │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                      CLASSIFIER                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Dropout(0.1)                                            │  │
│  │  Linear(768 → num_classes)                               │  │
│  │                                                           │  │
│  │  Output: [batch, aspects, 3]                             │  │
│  │          (3 classes: negative, neutral, positive)        │  │
│  └──────────────────────────────────────────────────────────┘  │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                        OUTPUT                                   │
│  Predictions: [positive, negative]                              │
│  Gate Statistics: {mean: 0.12, sparsity: 72%, ...}             │
└─────────────────────────────────────────────────────────────────┘
```

---

### 2. Sentence-Level 任務架構（IMDB 等）

```
┌─────────────────────────────────────────────────────────────────┐
│                     INPUT (Sentence-Level)                      │
│  Text: "This movie is amazing! The acting was superb and        │
│         the plot engaging."                                     │
│  Label: positive                                                │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BERT EMBEDDING LAYER                         │
│  ┌──────────────┐                                               │
│  │ Text Encoder │                                               │
│  │  (DistilBERT)│                                               │
│  └──────┬───────┘                                               │
│         │  [batch, seq_len, 768]                                │
└─────────┼───────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│          IMPLICIT ASPECT DISCOVERY MODULE (創新！)               │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Learnable Aspect Queries (5 個可學習的查詢向量)          │  │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐      │  │
│  │  │overall  │  │story/   │  │acting   │  │technical│ ...  │  │
│  │  │impress. │  │plot     │  │perform. │  │quality  │      │  │
│  │  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘      │  │
│  │       │            │            │            │            │  │
│  │       └────────────┴────────────┴────────────┘            │  │
│  │                    │                                       │  │
│  │       ┌────────────▼────────────┐                         │  │
│  │       │ Cross-Attention         │                         │  │
│  │       │ (Queries attend to Text)│                         │  │
│  │       └────────────┬────────────┘                         │  │
│  │                    │                                       │  │
│  │  Output:                                                  │  │
│  │  - Implicit Aspects: [batch, 5, 768]                     │  │
│  │  - Importance Weights: [batch, 5]                        │  │
│  │  - Attention Maps: [batch, 5, seq_len]                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  預定義的隱含 Aspects（針對不同領域）:                            │
│  - Movie Review: [overall, story, acting, technical, emotion]  │
│  - Product Review: [overall, quality, value, features, usability]│
│  - Restaurant: [overall, food, service, ambiance, value]       │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│              PMAC (對隱含 Aspects 進行組合)                      │
│  使用 Selective PMAC 學習哪些隱含 aspects 應該互相影響           │
│  Output: [batch, 5, 768] + Gate Matrix                         │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│              IARM (建模隱含 Aspects 間的關係)                    │
│  Output: Enhanced Features [batch, 5, 768]                     │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│              FUSION LAYER (融合為句子級別表示)                   │
│                                                                 │
│  ┌──────────────────┐    ┌──────────────┐    ┌──────────────┐ │
│  │ Mean Pooling     │ OR │ Max Pooling  │ OR │ Weighted     │ │
│  │                  │    │              │    │ Pooling      │ │
│  └──────────────────┘    └──────────────┘    └──────────────┘ │
│                                                                 │
│  Weighted Pooling (推薦):                                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  weights = Attention(aspects)  # 學習每個 aspect 的權重  │  │
│  │  sentence_repr = Σ(weights[i] × aspects[i])              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Output: [batch, 768]                                          │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                      CLASSIFIER                                 │
│  Output: [batch, 2] (binary: negative/positive)                │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                        OUTPUT                                   │
│  Prediction: positive                                           │
│  Aspect Analysis: {overall: 0.8, story: 0.9, acting: 0.85, ...}│
│  Gate Statistics: {mean: 0.15, sparsity: 65%, ...}             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 模組詳解

### 1. BERT Embedding Layer

```
檔案: models/bert_embedding.py
功能: 將文字轉換為語義向量

Input:  Text tokens [batch, seq_len]
        Aspect tokens [batch, aspects, aspect_len]

Output: Text embeddings [batch, seq_len, 768]
        Aspect embeddings [batch, aspects, 768]

參數:
- model_name: 'distilbert-base-uncased' (預設)
- freeze_bert: False (可微調) / True (凍結)
```

### 2. AAHA (Aspect-Aware Hierarchical Attention)

```
檔案: models/aaha_enhanced.py
功能: 為每個 aspect 提取相關的上下文

三層注意力機制:
┌──────────────────┐
│ Word-level       │ → 捕捉關鍵詞 ("great", "terrible")
├──────────────────┤
│ Phrase-level     │ → 捕捉片語 ("very good", "not bad")
├──────────────────┤
│ Sentence-level   │ → 捕捉整體語境
└──────────────────┘

Input:  Text hidden [batch, seq_len, 768]
        Aspect repr [batch, 768]

Output: Context vector [batch, 768]
        Attention weights (可視化用)
```

### 3. PMAC (Progressive Multi-Aspect Composition)

#### 3.1 Sequential PMAC (傳統版本)

```
檔案: models/pmac_enhanced.py
功能: 順序組合 aspects

流程:
aspect_1 →
aspect_1 + aspect_2 →
aspect_1 + aspect_2 + aspect_3 → ...

特點: 固定組合順序，所有 aspects 強制互相影響
```

#### 3.2 Selective PMAC (創新版本) ⭐

```
檔案: models/pmac_selective.py
功能: 可學習的選擇性組合

核心創新:
┌─────────────────────────────────────────┐
│ Gate Network (決定是否組合)              │
│ Input: concat([aspect_i, aspect_j])     │
│ Output: gate_value ∈ [0, 1]             │
│                                         │
│ gate ≈ 0: aspects 保持獨立               │
│ gate ≈ 1: aspects 強烈互相影響           │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Composition Network (如何組合)           │
│ Input: concat([aspect_i, aspect_j])     │
│ Output: composed_feature                │
└─────────────────────────────────────────┘

最終公式:
aspect_i_final = aspect_i + Σ(gate_ij × Composition(aspect_i, aspect_j))
                             j≠i

輸出:
- Composed features [batch, aspects, 768]
- Gate matrix [batch, aspects, aspects]
```

**Gate 初始化策略:**
```python
# 偏向稀疏性（大部分 gate 接近 0）
bias = -2.0  # sigmoid(-2.0) ≈ 0.12
```

### 4. IARM (Inter-Aspect Relation Modeling)

```
檔案: models/iarm_enhanced.py
功能: 建模 aspects 間的高階交互關係

三種模式:

1. Transformer (推薦)
   ┌─────────────────────────────┐
   │ Multi-Head Self-Attention   │
   │ Feed-Forward Network        │
   └─────────────────────────────┘

2. GAT (Graph Attention Network)
   ┌─────────────────────────────┐
   │ 將 aspects 視為圖節點       │
   │ 學習邊的權重                │
   └─────────────────────────────┘

3. Bilinear
   ┌─────────────────────────────┐
   │ 雙線性交互                  │
   │ aspect_i^T W aspect_j       │
   └─────────────────────────────┘

參數:
- num_heads: 4 (注意力頭數)
- num_layers: 2 (堆疊層數)
- relation_mode: 'transformer' / 'gat' / 'bilinear'
```

### 5. Implicit Aspect Discovery (句子級別專用)

```
檔案: models/implicit_aspect_discovery.py
功能: 從無標註文本中自動發現語義面向

核心機制:
┌─────────────────────────────────────────┐
│ Learnable Aspect Queries                │
│ (類似 DETR 的 Object Queries)           │
│                                         │
│ queries = nn.Parameter([5, 768])        │
│ 訓練過程中自動學習代表不同語義面向       │
└─────────────────────────────────────────┘

Cross-Attention:
Query (aspect queries) × Key/Value (text tokens)
→ 每個 query 關注文本中與其語義相關的部分

輸出:
- Aspect representations [batch, num_aspects, 768]
- Importance weights [batch, num_aspects]
- Attention maps [batch, num_aspects, seq_len] (可視化用)
```

### 6. Loss Functions

```
檔案: utils/focal_loss.py

支援三種損失函數:

1. Cross-Entropy (標準)
   Loss = -log(p_correct_class)

2. Focal Loss (處理類別不平衡)
   FL = -α(1-p)^γ log(p)

   參數:
   - gamma: 2.0 (聚焦難分樣本)
   - alpha: [1.0, 3.0, 1.0] (類別權重)

3. Adaptive Loss (自動調整權重)
   根據訓練過程中的類別表現動態調整權重
```

---

## 資料流程

### Aspect-Based 任務完整流程

```python
# 1. 資料載入
train_samples, test_samples = load_multiaspect_data(
    train_path, test_path,
    min_aspects=2,
    max_aspects=8
)

# 2. 模型建構
model = HMACNetMultiAspect(
    bert_model_name='distilbert-base-uncased',
    use_pmac=True,
    pmac_composition_mode='selective',
    use_iarm=True,
    iarm_relation_mode='transformer'
)

# 3. 前向傳播
logits, gate_stats = model(
    text_input_ids,      # [batch, seq_len]
    text_attention_mask, # [batch, seq_len]
    aspect_input_ids,    # [batch, max_aspects, aspect_len]
    aspect_attention_mask, # [batch, max_aspects, aspect_len]
    aspect_mask          # [batch, max_aspects]
)

# 4. 損失計算
loss = focal_loss(
    logits,        # [batch, max_aspects, 3]
    labels,        # [batch, max_aspects]
    aspect_mask,   # [batch, max_aspects]
    class_weights=[1.0, 3.0, 1.0]
)

# 5. 反向傳播
loss.backward()
optimizer.step()

# 6. 評估
predictions = torch.argmax(logits, dim=-1)
metrics = compute_metrics(predictions, labels)
```

### Sentence-Level 任務完整流程

```python
# 1. 資料載入（通過配置系統）
from data.dataset_manager import DatasetManager

manager = DatasetManager()
train, val, test = manager.load_dataset('imdb')

# 2. 模型建構
model = HMACNetSentenceLevel(
    num_classes=2,  # binary classification
    domain='movie_review',
    num_implicit_aspects=5,
    use_pmac=True,
    use_iarm=True,
    fusion_strategy='weighted_pooling'
)

# 3. 前向傳播
logits, aspect_info = model(
    text_input_ids,
    text_attention_mask,
    return_aspect_info=True  # 用於分析
)

# 4. 可解釋性分析
aspect_repr = aspect_info['aspect_representations']  # [batch, 5, 768]
aspect_importance = aspect_info['aspect_importance'] # [batch, 5]
aspect_attn = aspect_info['aspect_attention']        # [batch, 5, seq_len]
gate_stats = aspect_info['gate_stats']               # Gate matrix

# 5. 視覺化哪些詞彙被每個 aspect 關注
for i, aspect_name in enumerate(aspect_info['aspect_names']):
    top_words = get_top_attended_words(
        tokens,
        aspect_attn[:, i, :],
        top_k=5
    )
    print(f"{aspect_name}: {top_words}")
```

---

## 創新點

### 1. Selective PMAC ⭐⭐⭐

**問題**: 傳統 PMAC 強制組合所有 aspects，導致互相干擾

**創新**: 可學習的 Gate 機制

```
傳統 PMAC:
aspect_final = Compose(aspect_1, aspect_2, aspect_3, ...)
→ 所有 aspects 強制互相影響

Selective PMAC:
aspect_i = aspect_i + Σ(gate_ij × Compose(aspect_i, aspect_j))
                       j≠i
→ gate 自動學習是否需要組合
→ 大部分 gate ≈ 0 (保持獨立)
→ 少數 gate > 0.5 (選擇性組合)
```

**實驗結果**:
- Sparsity: 72.3% (aspects 保持獨立)
- 提升 Aspect-level F1: +3.2%

### 2. Implicit Aspect Discovery ⭐⭐

**問題**: 句子級別資料沒有 aspect 標註，無法使用原有架構

**創新**: 自動發現隱含的語義面向

```
傳統句子級別模型:
text → BERT → [CLS] → classifier
→ 直接端到端，無中間表示

我們的方法:
text → BERT → Implicit Aspect Discovery → PMAC → IARM → classifier
→ 自動發現 5 個語義面向
→ 保留 PMAC 和 IARM 創新
→ 提供可解釋性
```

**優勢**:
- 可視化模型關注的語義面向
- 跨領域遷移（預定義 aspect queries）
- 統一框架處理兩種任務

### 3. 統一的資料集管理系統 ⭐

**問題**: 不同資料集格式不同，難以管理

**創新**: JSON 配置 + 自動載入器

```json
// dataset_config.json
{
  "imdb": {
    "type": "sentence_level",
    "paths": {"train": "...", "test": "..."},
    "format": "imdb_folder"
  },
  "semeval_rest": {
    "type": "aspect_based",
    "paths": {"train": "...", "test": "..."},
    "format": "xml"
  }
}
```

**使用**:
```bash
# 只需改變 dataset 代號
python train_sentence_level.py --dataset imdb
python train_sentence_level.py --dataset sst2
python train_multiaspect.py  # SemEval (預設)
```

---

## 支援的任務類型

### 任務 1: Aspect-Based Sentiment Analysis

```
資料集: SemEval-2014 Restaurant/Laptop

範例:
Input:
  Text: "The food was excellent but the service was slow."
  Aspects: ["food", "service"]

Output:
  food: positive
  service: negative

訓練命令:
python experiments/train_multiaspect.py \
  --use_pmac --pmac_mode selective \
  --use_iarm --iarm_mode transformer
```

### 任務 2: Sentence-Level Sentiment Analysis

```
資料集: IMDB, SST-2, Yelp, Amazon Reviews

範例:
Input:
  Text: "This movie is amazing! The acting was superb."

Output:
  Label: positive
  Aspect Analysis:
    - overall impression: 0.92
    - story/plot: 0.65
    - acting performance: 0.95
    - technical quality: 0.71
    - emotional impact: 0.88

訓練命令:
python experiments/train_sentence_level.py \
  --dataset imdb \
  --use_pmac --use_iarm \
  --num_implicit_aspects 5
```

### 任務 3: 自定義文本分類

```
支援任何 CSV/JSON 格式的文本分類資料

步驟:
1. 準備資料 (CSV/JSON)
2. 在 dataset_config.json 添加配置
3. 執行訓練

範例配置:
{
  "my_dataset": {
    "type": "sentence_level",
    "paths": {"train": "data/my_data/train.csv"},
    "format": "csv",
    "text_column": "text",
    "label_column": "sentiment"
  }
}

訓練:
python experiments/train_sentence_level.py --dataset my_dataset
```

---

## 檔案結構

```
2026_Thesis_v4/
├── data/
│   ├── dataset_config.json              # 資料集配置
│   ├── dataset_manager.py               # 資料集管理器
│   ├── semeval_multiaspect.py           # SemEval 載入器
│   ├── sentence_level_dataset.py        # 句子級別載入器
│   └── multiaspect_dataset.py           # PyTorch Dataset
│
├── models/
│   ├── bert_embedding.py                # BERT 編碼器
│   ├── aaha_enhanced.py                 # AAHA 模組
│   ├── pmac_enhanced.py                 # 傳統 PMAC
│   ├── pmac_selective.py                # Selective PMAC ⭐
│   ├── iarm_enhanced.py                 # IARM 模組
│   ├── implicit_aspect_discovery.py     # 隱含 Aspect 發現 ⭐
│   ├── hmacnet_sentence_level.py        # 句子級別模型
│   └── base_model.py                    # 基礎類別
│
├── experiments/
│   ├── train_multiaspect.py             # Aspect-based 訓練
│   └── train_sentence_level.py          # Sentence-level 訓練
│
├── utils/
│   ├── focal_loss.py                    # Focal Loss 實現
│   └── ...
│
├── docs/
│   ├── SYSTEM_ARCHITECTURE.md           # 本文檔
│   ├── SENTENCE_LEVEL_GUIDE.md          # 句子級別使用指南
│   └── ...
│
└── QUICK_START.md                       # 快速開始指南
```

---

## 技術規格

### 硬體需求

| 項目 | 最低配置 | 推薦配置 |
|------|---------|---------|
| GPU | 無（可用 CPU） | NVIDIA GPU (4GB+ VRAM) |
| RAM | 8GB | 16GB+ |
| 儲存 | 5GB | 10GB+ |

### 軟體依賴

```python
torch >= 1.9.0
transformers >= 4.0.0
scikit-learn >= 0.24.0
numpy >= 1.19.0
pandas >= 1.2.0
matplotlib >= 3.3.0
seaborn >= 0.11.0
tqdm >= 4.60.0
```

### 訓練時間估算

| 任務 | 資料集 | Epochs | 時間 (GPU) | 時間 (CPU) |
|------|--------|--------|-----------|-----------|
| Aspect-based | SemEval | 30 | ~1.5 小時 | ~6 小時 |
| Sentence-level | IMDB (限制500樣本) | 3 | ~5 分鐘 | ~20 分鐘 |
| Sentence-level | IMDB (完整) | 20 | ~3 小時 | ~12 小時 |

---

## 總結

### 系統特色

✅ **模組化設計**: 每個模組可獨立替換或消融
✅ **雙任務支援**: Aspect-based + Sentence-level
✅ **可解釋性**: Gate 值、Attention 權重可視化
✅ **通用性**: 支援任何文本分類資料
✅ **易用性**: 單行命令啟動訓練

### 核心貢獻

1. **Selective PMAC**: 可學習的 aspect 組合機制
2. **Implicit Aspect Discovery**: 無監督語義面向發現
3. **統一框架**: 同一套創新模組處理不同任務

---

**版本歷史**:
- v2.0 (2025-01-12): 新增 Sentence-level 支援、Implicit Aspect Discovery
- v1.0 (2025-01-10): 初始版本，Aspect-based 任務

**維護者**: Multi-Aspect HMAC-Net Team
