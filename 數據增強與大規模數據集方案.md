# 數據增強與大規模數據集方案

## 📋 目錄

1. [將單面向轉為多面向的方法](#1-將單面向轉為多面向的方法)
2. [大規模多面向數據集](#2-大規模多面向數據集)
3. [推薦方案](#3-推薦方案)

---

## 1. 將單面向轉為多面向的方法

### 1.1 基於 LLM 的數據增強 ⭐ **最推薦**

#### 方法 A: Multi-faceted Data Augmentation (MDA)

**論文**: "Multi-faceted data augmentation for aspect-based sentiment analysis via large language models" (2025)

**核心思路**:
使用 GPT-4/Claude 等 LLM 將單面向句子擴展為多面向

**實施步驟**:

```python
# 偽代碼示例
prompt = f"""
Given a review: "{single_aspect_review}"
Current aspect: "{aspect}" with sentiment: "{sentiment}"

Please:
1. Add 1-2 more aspects to this review while keeping the original aspect
2. Assign different sentiments to the new aspects
3. Keep the review natural and coherent

Output format:
Review: [new multi-aspect review]
Aspects: [(aspect1, sentiment1), (aspect2, sentiment2), ...]
"""

response = llm.generate(prompt)
```

**優點**:
- ✅ 生成品質高，語義連貫
- ✅ 可控制生成的 aspect 數量
- ✅ 支援對比/因果關係生成

**缺點**:
- ❌ 需要 LLM API 成本
- ❌ 生成速度較慢

**實際範例**:

```
原始 (Restaurants 單面向):
"The food was amazing."
Aspect: food, Sentiment: positive

LLM 生成 (多面向):
"The food was amazing, but the service was quite slow."
Aspects: [(food, positive), (service, negative)]
```

---

#### 方法 B: Contrastive Cross-Channel Data Augmentation (C³DA)

**論文**: "A Contrastive Cross-Channel Data Augmentation Framework for Aspect-based Sentiment Analysis" (2022)

**核心思路**:
通過兩個通道生成多面向樣本：
1. **Aspect Augmentation Channel**: 給定情感，生成新的 aspect
2. **Polarity Augmentation Channel**: 給定 aspect，生成對比情感

**實施流程**:

```
步驟 1: Aspect Augmentation
輸入: (positive sentiment)
生成: "The food was great" + "service" aspect
輸出: "The food was great, and the service was excellent"

步驟 2: Polarity Augmentation
輸入: (food, positive) + (service, negative)
生成: "The food was great, but the service was terrible"
```

**優點**:
- ✅ 專注對比關係（IARN 最需要）
- ✅ 可訓練領域內生成器
- ✅ 結合 contrastive learning 提升效果

---

### 1.2 基於規則的方法

#### 方法 C: Template-based Generation

**適用場景**: Restaurants/Laptop 等結構化領域

**實施方式**:

```python
# 對比模板
templates = [
    "{aspect1} {sent1_word}, but {aspect2} {sent2_word}",
    "While {aspect1} {sent1_word}, {aspect2} {sent2_word}",
    "The {aspect1} was {sent1_word}, however the {aspect2} was {sent2_word}"
]

# 情感詞庫
positive_words = ["great", "excellent", "amazing", "wonderful"]
negative_words = ["terrible", "awful", "poor", "disappointing"]

# 領域 aspect 庫
restaurant_aspects = ["food", "service", "ambiance", "price", "location"]

# 生成
for aspect1, aspect2 in combinations(restaurant_aspects, 2):
    for template in templates:
        new_review = template.format(
            aspect1=aspect1,
            aspect2=aspect2,
            sent1_word=random.choice(positive_words),
            sent2_word=random.choice(negative_words)
        )
```

**範例輸出**:
```
"The food was excellent, but the service was disappointing."
"While the ambiance was wonderful, the price was terrible."
```

**優點**:
- ✅ 實施簡單，成本低
- ✅ 速度快，可大量生成

**缺點**:
- ❌ 多樣性受限
- ❌ 可能不夠自然

---

### 1.3 將句子級（IMDB）轉為多面向

#### 挑戰
IMDB 是**電影評論**，句子級情感（positive/negative），**沒有 aspect 標註**

#### 解決方案 1: 自動 Aspect 提取 + LLM 重寫

**步驟**:

```
1. 使用 Aspect Extraction 模型提取 implicit aspects
   輸入: "This movie was amazing with great acting."
   提取: [movie, acting]

2. 檢查是否為單 aspect
   如果只有 1 個 aspect → 進入步驟 3

3. 使用 LLM 添加對比 aspect
   Prompt: "Add one more movie aspect (plot/cinematography/music)
            with opposite sentiment"
   輸出: "This movie was amazing with great acting, but the plot was confusing."
```

#### 解決方案 2: 利用電影多維度特性

**電影評論的天然 aspects**:
- Plot (劇情)
- Acting (演技)
- Cinematography (攝影)
- Music/Soundtrack (音樂)
- Directing (導演)
- Special Effects (特效)

**實施**:

```python
# 使用 GPT-4 分解電影評論
prompt = f"""
Original review: "{imdb_review}"
Overall sentiment: {sentiment}

Please rewrite this into a multi-aspect review covering at least 2 aspects:
- Plot, Acting, Cinematography, Music, or Effects
- Assign different sentiments to different aspects
- Keep overall sentiment similar to original

Format:
Aspect 1: [aspect] - [sentiment] - [reason]
Aspect 2: [aspect] - [sentiment] - [reason]
Rewritten review: [natural multi-aspect review]
"""
```

**範例**:

```
原始 IMDB:
"This movie is terrible. Waste of time."
Sentiment: negative

LLM 生成多面向:
"This movie is terrible overall - the plot was confusing and dragged on.
However, I have to admit the cinematography was stunning."
Aspects: [(plot, negative), (cinematography, positive)]
```

---

## 2. 大規模多面向數據集

### 2.1 現有大規模數據集對比

| 數據集 | 規模 | 多面向比例 | 領域 | 年份 | 推薦度 |
|--------|------|-----------|------|------|--------|
| **MAMS-ATSA** | 13,854 句 | **100%** | 餐廳 | 2019 | ⭐⭐⭐⭐⭐ **已使用** |
| **MEMD** | ~20,000 句 | **高** | 5 領域 | 2023 | ⭐⭐⭐⭐⭐ **強烈推薦** |
| **MuSe-CarASTE** | 28,295 句 | 高 | 汽車 | 2025 | ⭐⭐⭐⭐ |
| **FABSA** | ~10,500 評論 | 中 | 10 領域 | 2024 | ⭐⭐⭐ |
| **M-ABSA** | 大規模 | 中 | 7 領域/21 語言 | 2025 | ⭐⭐⭐ |
| **EduRABSA** | 中等 | 中 | 教育 | 2025 | ⭐⭐ |

---

### 2.2 各數據集詳細介紹

#### 🥇 MEMD-ABSA (最推薦) ⭐⭐⭐⭐⭐

**全名**: Multi-Element Multi-Domain Dataset for ABSA

**規模**:
- **~20,000 句評論**（是 MAMS 的 1.4 倍）
- **~30,000 quadruple 標註**
- 平均每句 1.56 個 quadruple

**特點**:
- ✅ **5 個領域**: Restaurant, Laptop, Phone, Camera, Car
- ✅ 支援 **explicit 和 implicit** aspects/opinions
- ✅ 支援完整的 ABSA 任務（AE, OE, ASTE, ASQE）
- ✅ 比 SemEval-2016 大 **3-5 倍**

**為何推薦給 IARN**:
1. **規模適中**: 20K 句，足夠訓練 IARN，不會像 Yelp (GB 級) 過大
2. **多領域**: 5 個領域可驗證 IARN 的泛化能力
3. **高品質標註**: Quadruple 標註包含完整的 aspect-opinion-sentiment 關係

**獲取方式**:
```bash
# GitHub
git clone https://github.com/NUSTM/MEMD-ABSA
```

**論文**:
- ArXiv: https://arxiv.org/abs/2306.16956
- Published: Language Resources and Evaluation (2025)

---

#### 🥈 MuSe-CarASTE ⭐⭐⭐⭐

**全名**: Multi-Sentiment Automotive Aspect Sentiment Triplet Extraction

**規模**:
- **28,295 句** (最大規模！)
- 來自 5,500 個汽車評論視頻片段

**特點**:
- ✅ 汽車領域（垂直領域，專業性強）
- ✅ 視頻轉錄文本（更口語化）
- ✅ ASTE 任務（aspect, opinion, sentiment triplets）

**為何推薦**:
- 最大規模的 ASTE 數據集
- 新興領域（2025），競爭少

**限制**:
- ❌ 單一領域（汽車）
- ❌ 可能包含較多單 aspect（需驗證）

---

#### 🥉 FABSA ⭐⭐⭐

**全名**: Feedback Aspect-Based Sentiment Analysis

**規模**:
- **~10,500 評論**
- 跨 10 個領域

**領域**:
Fashion, Consulting, Travel Booking, Ride-hailing, Banking, Trading, E-commerce, Food Delivery, etc.

**特點**:
- ✅ 多領域（10 個）
- ✅ 真實用戶反饋
- ✅ 來自 Trustpilot, Google Play, Apple Store

**為何推薦**:
- 領域多樣性高
- 真實世界應用場景

**限制**:
- ❌ 規模較小（與 MAMS 相當）
- ❌ 多面向比例未知

---

### 2.3 Yelp/Amazon 數據集

#### Yelp Dataset

**規模**:
- **6.99M 評論**
- **150K 商家**
- **3.9 GB** JSON 文件

**獲取**:
```
https://www.yelp.com/dataset
```

**問題**:
- ❌ **沒有 aspect 標註**（需要自己標註或使用 LLM 生成）
- ❌ 規模過大（訓練成本高）
- ❌ 單 aspect 比例高（需要篩選/增強）

**使用建議**:
1. **子集抽樣**: 抽取 10K-50K 餐廳評論
2. **LLM 標註**: 使用 GPT-4 自動標註 aspects
3. **篩選多面向**: 只保留包含 2+ aspects 的評論

---

#### Amazon Reviews 2023

**規模**:
- **571.54M 評論**
- **48.19M 用戶**
- **多個產品類別**

**獲取**:
```
https://huggingface.co/datasets/McAuley-Lab/Amazon-Reviews-2023
```

**問題**:
- ❌ 同樣沒有 aspect 標註
- ❌ 規模極大（處理困難）

---

## 3. 推薦方案

### 方案 A: 使用 MEMD 擴展實驗 ⭐⭐⭐⭐⭐

**適合論文的最佳方案**

**實施步驟**:

```bash
# 1. 下載 MEMD 數據集
git clone https://github.com/NUSTM/MEMD-ABSA

# 2. 轉換為你的數據格式
python scripts/convert_memd_to_format.py

# 3. 訓練 IARN on MEMD
python experiments/train_from_config.py --config configs/iarn_memd.yaml --dataset memd

# 4. 對比結果
python experiments/generate_comprehensive_report.py --dataset memd
```

**預期結果**:

| 數據集 | 規模 | 多面向% | IARN 預期 F1 | 論文價值 |
|--------|------|---------|-------------|---------|
| MAMS | 4,297 | 100% | 0.8400 ✅ | 主實驗 |
| MEMD | 20,000 | 高 | **0.82-0.85** | 驗證泛化能力 |
| Restaurants | 1,500 | 20% | 0.7090 | 消融研究 |

**論文中的價值**:
```
我們在三個數據集上驗證 IARN：
- MAMS (100% 多面向): 驗證核心創新
- MEMD (多領域，大規模): 驗證泛化能力
- Restaurants (20% 多面向): 消融研究，證明專用性
```

**優點**:
- ✅ 規模大（20K），訓練充分
- ✅ 多領域（5 個），泛化性強
- ✅ 高品質標註
- ✅ 已發表，可信度高

---

### 方案 B: LLM 增強 Restaurants 數據 ⭐⭐⭐⭐

**提升 Restaurants 上的 IARN 性能**

**實施步驟**:

```python
# 1. 使用 GPT-4 將單 aspect 轉為多 aspect
def augment_single_to_multi(review, aspect, sentiment):
    prompt = f"""
    Restaurant review: "{review}"
    Current aspect: {aspect} ({sentiment})

    Add ONE more aspect (food/service/ambiance/price) with OPPOSITE sentiment.
    Keep it natural and under 20 words.

    Output:
    New review: [...]
    New aspects: [(aspect1, sent1), (aspect2, sent2)]
    """

    response = gpt4.generate(prompt)
    return parse_response(response)

# 2. 過濾增強數據
# - Confidence filtering: 只保留高品質樣本
# - Diversity check: 避免重複模式

# 3. 混合訓練
train_data = original_data + augmented_data
```

**預期提升**:
- Restaurants F1: 0.7090 → **0.73-0.74** (+3-4%)
- 接近或超過 Baseline (0.7220)

**成本估算**:
- Restaurants ~1,200 訓練樣本
- 假設 80% 需要增強 = 960 樣本
- GPT-4 成本: ~$5-10 (每千 tokens $0.03)

---

### 方案 C: 結合 MEMD + 增強 ⭐⭐⭐⭐⭐

**最全面的方案**

**實施**:

```
1. 在 MEMD (20K) 上訓練 IARN
   → 證明大規模多領域泛化能力

2. 使用 LLM 增強 Restaurants
   → 改善小數據集性能

3. 論文中呈現三組實驗：
   - MAMS: 核心創新驗證 (F1 = 0.8400)
   - MEMD: 大規模泛化 (F1 ≈ 0.83)
   - Restaurants (增強): 數據增強有效性 (F1 ≈ 0.73)
```

**論文結構**:

```markdown
Section 4: Experiments

4.1 Main Results (MAMS)
    - IARN achieves SOTA (F1 = 0.8400)

4.2 Multi-Domain Generalization (MEMD)
    - IARN maintains strong performance across 5 domains
    - Proves scalability to larger datasets

4.3 Ablation Study
    4.3.1 Dataset Characteristics (Restaurants)
          - Shows IARN is designed for multi-aspect scenarios

    4.3.2 Data Augmentation (Restaurants + LLM)
          - LLM-based augmentation improves performance
          - Validates the importance of multi-aspect data
```

---

## 4. 具體實施建議

### 優先順序

#### 短期（1-2 週）
1. ✅ **下載並轉換 MEMD 數據集**
2. ✅ **在 MEMD 上訓練 IARN**
3. ✅ **生成 MEMD 實驗報告**

#### 中期（2-4 週）
4. ⭐ **實施 LLM 增強 Restaurants**
   - 使用 GPT-4 API
   - 設計高品質 prompts
   - 過濾生成數據

#### 長期（可選，如有時間）
5. 📊 **探索 MuSe-CarASTE**（汽車領域）
6. 🌍 **嘗試 M-ABSA**（多語言）

---

### 工具推薦

#### LLM API
```python
# OpenAI GPT-4
import openai
openai.api_key = "your-key"

response = openai.ChatCompletion.create(
    model="gpt-4-turbo",
    messages=[{"role": "user", "content": prompt}],
    temperature=0.7
)
```

#### 開源替代（成本敏感）
```python
# Llama 3 70B (via HuggingFace)
from transformers import pipeline

generator = pipeline("text-generation", model="meta-llama/Llama-3-70b")
response = generator(prompt, max_new_tokens=100)
```

---

## 5. 總結

### 🎯 最推薦方案

**方案 A + C 的組合**:

1. **立即實施 MEMD 實驗**
   - 成本: 低（只需下載數據）
   - 收益: 高（論文必需的泛化實驗）
   - 時間: 1-2 週

2. **之後實施 LLM 增強**（如有時間/預算）
   - 成本: 中（~$10 API）
   - 收益: 中（改善 Restaurants 結果）
   - 時間: 1-2 週

### 📊 預期論文實驗表格

| 數據集 | 規模 | 多面向% | Baseline | Method 1 | IARN | 改進 |
|--------|------|---------|----------|----------|------|------|
| **MAMS** | 4,297 | 100% | 0.8217 | 0.8349 | **0.8400** | +2.2% ⭐ |
| **MEMD** | 20,000 | 高 | TBD | TBD | **~0.83** | TBD |
| **Restaurants** | 1,500 | 20% | 0.7220 | 0.7228 | 0.7090 | -1.3% |
| **Restaurants (增強)** | ~3,000 | ~60% | TBD | TBD | **~0.73** | TBD |

### ✅ 行動清單

- [ ] 下載 MEMD 數據集
- [ ] 創建 `configs/iarn_memd.yaml`
- [ ] 轉換 MEMD 到你的數據格式
- [ ] 訓練 IARN on MEMD
- [ ] 生成 MEMD 報告
- [ ] 設計 LLM augmentation prompts
- [ ] 增強 Restaurants 數據
- [ ] 重新訓練 IARN on Restaurants (增強版)

---

**創建日期**: 2025-11-21
**下一步**: 下載並探索 MEMD 數據集
